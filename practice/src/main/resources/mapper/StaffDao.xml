<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hodo.practice.dao.StaffDao">

    <resultMap type="com.hodo.practice.entity.Staff" id="StaffMap">
        <result property="jobId" column="job_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="birthday" column="birthday" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="mobile" column="mobile" jdbcType="VARCHAR"/>
        <result property="post" column="post" jdbcType="INTEGER"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StaffMap">
        select job_id,
               name,
               birthday,
               sex,
               mobile,
               post,
               password,
               role
        from practice2.staff
        where job_id = #{jobId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="StaffMap">
        select job_id,
               name,
               birthday,
               sex,
               mobile,
               post,
               password,
               role
        from practice2.staff limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="StaffMap">
        select
        job_id, name, birthday, sex, mobile, post, password, role
        from practice2.staff
        <where>
            <if test="jobId != null">
                and job_id = #{jobId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="birthday != null and birthday != ''">
                and birthday = #{birthday}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="mobile != null and mobile != ''">
                and mobile = #{mobile}
            </if>
            <if test="post != null">
                and post = #{post}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="jobId" useGeneratedKeys="true">
        insert into practice2.staff(name, birthday, sex, mobile, post, password, role)
        values (#{name}, #{birthday}, #{sex}, #{mobile}, #{post}, #{password}, #{role})
    </insert>

    <insert id="insertBatch" keyProperty="jobId" useGeneratedKeys="true">
        insert into practice2.staff(name, birthday, sex, mobile, post, password, role)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.birthday}, #{entity.sex}, #{entity.mobile}, #{entity.post}, #{entity.password},
            #{entity.role})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="jobId" useGeneratedKeys="true">
        insert into practice2.staff(name, birthday, sex, mobile, post, password, role)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.birthday}, #{entity.sex}, #{entity.mobile}, #{entity.post}, #{entity.password},
            #{entity.role})
        </foreach>
        on duplicate key update
        name = values(name) , birthday = values(birthday) , sex = values(sex) , mobile = values(mobile) , post =
        values(post) , password = values(password) , role = values(role)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update practice2.staff
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="birthday != null and birthday != ''">
                birthday = #{birthday},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="mobile != null and mobile != ''">
                mobile = #{mobile},
            </if>
            <if test="post != null">
                post = #{post},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
        </set>
        where job_id = #{jobId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from practice2.staff
        where job_id = #{jobId}
    </delete>

</mapper>

